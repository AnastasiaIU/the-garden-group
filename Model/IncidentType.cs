namespace Model
{
    /// <summary>
    /// Enumeration representing the types of incidents that can be reported within the organization.
    /// </summary>
    public enum IncidentType
    {
        Software,
        Hardware,
        Service
    }
}