namespace Model
{
    /// <summary>
    /// Enumeration representing the different roles an employee can have within the organization.
    /// </summary>
    public enum EmployeeRole
    {
        RegularEmployee,
        ServiceDeskEmployee
    }
}